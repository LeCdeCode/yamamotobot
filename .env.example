# Ton token bot Discord (obligatoire)
BOT_TOKEN=ton_token_ici

# Optionnel : chemin où les données JSON sont stockées
# DATA_DIR=/data

# Optionnel : active un serveur HTTP keep-alive (pour Render free tier)
# KEEP_ALIVE=true
# PORT=8080
